version: 2.2.{build}

pull_requests:
  do_not_increment_build_number: true

configuration: Release

platform:
- x86
- x64

build:
  project: win32/nx.sln
  verbosity: minimal

after_build:
 - ps: .\win32\win.package.ps1